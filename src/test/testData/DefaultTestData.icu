en {
    homePage {
        title { "Hello World" }

        paragraphs {

            test      { "{t, plural, one {test} two {test2}}" }
            someOther { "test" }

            welcome {
                "Welcome back {userName}, you have "
                "{unreadMsg, plural, "
                    "=0 {"
                        "no unread messages"
                    "}"
                    "one   {# unread message}"
                    "other {# unread messages}"
                "}."
            }

            rights {
                "{adminLevel, select, "
                    "admin   {Site Administrator}"
                    "support {Support Staff}"
                    "user    {User}"
                    "other   {Guest}"
                "}"
            }
        }

        aBlock { }

        test  { "Another Test" }
    }
}
